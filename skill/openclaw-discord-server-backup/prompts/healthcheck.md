# Healthcheck Prompt

Check:
- config JSON loads
- state JSON loads
- queue JSON loads
- backup root exists
- scripts compile
- active queue count
- last audit result if available

Report blockers only.
